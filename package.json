{
  "name": "NELs",
  "version": "0.0.5",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "tap tests/*.js ",
    "build": "browserify index.js -o bundle.js",
    "build:watch": "watch 'npm run build' ./src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "faucet": "0.0.1",
    "tap": "~6.3.0",
    "tape": "~4.6.0",
    "watch": "^0.19.2"
  }
}
